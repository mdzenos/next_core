import serverApiClient from '@/services/serverApiClient';

export const {{camelCase name}}Service = {
  async fetchData(params?: any) {
    try {
      const response = await serverApiClient.get('/{{kebabCase name}}', { params });
      return response.data;
    } catch (error) {
      console.error('Error fetching {{camelCase name}}:', error);
      throw error;
    }
  },

  // TODO: Add more API methods here
};
